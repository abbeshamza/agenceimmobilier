{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DemandeVisite</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ entity.email }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ entity.tel }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ entity.horaire }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ entity.texte }}</td>
            </tr>
            <tr>
                <th>Annonce</th>
                <td>{{ entity.annonce }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('demandevisite') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('demandevisite_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
